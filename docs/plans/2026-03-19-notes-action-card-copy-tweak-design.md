# Notes Action Card Copy Tweak Design

## Goal

Tighten the copy on the two Notes action cards so they read more cleanly.

## Changes

- Remove the subtitle from the `New folder` card.
- Rename `Add new note` to `Add Note`.
- Remove the subtitle from the `Add Note` card.

## Scope

- Only update the action-card copy in `src/pages/Notes/Notes.tsx`.
- Do not change layout, interactions, or modal behavior.
